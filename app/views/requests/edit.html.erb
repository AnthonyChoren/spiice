<div class="mt-3 m-3">
</div>

<div class="container">
  <div class="row">
    <div class="col-sm-8 offset-sm-2">
      <p>Are you sure you want to accept the request of <%= @request.user.first_name %> <%= @request.user.last_name %>?</p>
      <div class="d-flex justify-content-end">
        <%= link_to "Profile of designer", user_path(@request.user), class: "btn btn-primary"%>
      </div>
      <hr>
      <%= simple_form_for @request do |f| %>
          <%= f.input :answer,
                input_html: { autocomplete: "message" },
                required: true,
                autofocus: true,
                hint: 'One request per project'%>
      <%= f.submit "Yes, I accept the request", class: "btn btn-primary2" %>
      <% end %>
      <%= link_to "Cancel", requests_path, class: "btn btn-primary3"%>

    </div>
  </div>
</div>
