<!-- <div class="pl-2 pb-0 mt-2 d-flex justify-content-start align-items-center">
  <a href="<%= projects_path %>" class="ri-arrow-left-circle-line back-icon"></a>
  <div>
    <%#= link_to "See all projects", projects_path, class: "btn-back pl-1" %>
  </div> -->
<!-- </div> -->
<div class="mt-3 m-3 d-flex justify-content-between align-items-center">
  <div>
    <h3>Your Projects</h3>
  </div>
  <div class="mt-3 m-3">
    <%= link_to "Add a new project", new_admin_project_path, class: "btn btn-grey"%>
  </div>
</div>

<div class="container mr-2">
  <% @projects.each do |project|  %>

  <div class="card mb-2">
    <div class="card-body">
      <p class="card-title"> <strong><%= project.title %></strong> </p>
      <p class="card-text"><%= project.description %></p>
      <p class="card-text">For a price of <%= project.price %> EUR</p>
      <p class="card-text"><%= project.deadline %></p>
      <%= link_to 'View details', project_path(project), class: 'btn btn-primary'  %>
      <br>
      <br>
      <%= link_to 'View your propositions for this project', requests_path(project), class: 'btn btn-primary'  %>
    </div>
  </div>
  <% end %>
</div>
<br>
<br>
<br>
<br>
<br>
