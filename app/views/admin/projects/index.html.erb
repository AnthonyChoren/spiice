<div class="container pb-3">
  <div class="row">
    <div class="col mt-1">
      <div>
        <h1 class="title">Dashboard</h1>
      </div>
      <div class="mt-3 d-flex justify-content-between align-items-center">
        <div>
          <h3 class="medium-title ml-1">My projects</h3>
        </div>
        <div>
          <%= link_to "Add a new project", new_admin_project_path, class: "btn btn-primary2"%>
        </div>
      </div>
    </div>
  </div>
  <% @projects.each do |project| %>
    <%= link_to project_path(project) do %>
      <div class="row mt-3 projects-card" style="color: #8C8C95">
        <div class="column">
          <% if project.user.photo.attached? %>
            <%= cl_image_tag project.user.photo.key, alt: "alttext", class: 'projects-card-img' %>
          <% else %>
            <%= image_tag "spiice.jpg", class: 'projects-card-img' %>
          <% end %>
        </div>
        <div class="column">
          <p class="projects-card-text"><%= project.description %></p>
          <h4 class="projects-card-title"><%= project.title %></h4>
          <p class="projects-card-price"><%= project.price %>$</p>
        </div>
      </div>
    <% end %>
  <% end %>
</div>
<div class="row bg-primary" style="height: 600px;">
    <div class=" ml-3 p-3">
      <h3 class="medium-title text-white">Notifications</h3>
    </div>
  </div>
</div>
